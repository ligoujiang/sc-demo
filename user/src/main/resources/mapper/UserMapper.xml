<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pxy.user.mapper.UserMapper">
    <update id="setPassword">
        UPDATE user SET password = #{password} WHERE id = #{id};
    </update>
    <select id="getUserByName" resultType="com.pxy.user.domain.po.User">
        select * from user where name=#{name}
    </select>
    <select id="getMenus" resultType="com.pxy.user.domain.po.Menu">
        select m.* from menu m
                            LEFT JOIN role_menu rm ON m.id=rm.menu_id
                            LEFT JOIN role r On rm.role_id=r.id
                            LEFT JOIN user_role ur On r.id=ur.role_id
                            LEFT JOIN user u On ur.user_id=u.id where u.id=#{id}
    </select>
    <select id="getUser" resultType="com.pxy.user.domain.vo.UserVO" parameterType="java.lang.Long">
        select * from user where id=#{id}
    </select>
</mapper>